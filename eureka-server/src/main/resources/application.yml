spring:
  application:
    name: eureka-server

server:
  port: 19090

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    #service-url:
    #  defaultZone: http://localhost:19090/eureka/
    service-url:
      defaultZone: http://eureka:19090/eureka/ #도커 컴포즈 사용시 설정
  #instance:
  #  hostname: localhost #로컬 테스트용 설정 (도커 컴포즈 사용시 지우기)


